# ========================================
# NovaMart Client - Environment Variables
# ========================================
# Copy this file to .env (and .env.production for prod build)

# Backend API URL
# - In dev (npm run dev): direct to Express server
# - In prod (Docker / Vercel): use empty string to call same-origin via nginx / relative path
VITE_API_URL=http://localhost:5000
